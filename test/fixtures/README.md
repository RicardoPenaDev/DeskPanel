# Fixtures compartilhadas

Fixtures usadas por testes de integração/E2E que atravessam os dois apps (PROJECT.md §15.3). Vazio até a Fase 2, quando o teste E2E local com `FakeExecutor` for implementado.
