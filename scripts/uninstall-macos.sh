#!/usr/bin/env bash
set -euo pipefail

# Desinstalador do DeskPanel Agent (PROJECT.md §14.2).
# Remove serviço e binário; preserva config.json/devices.json por padrão.
# Remoção de dados exige --purge explícito.
# Ainda não implementado — previsto para a Fase 5 (Instalação real).
echo "uninstall-macos.sh: ainda não implementado — ver Fase 5 em PROJECT.md" >&2
exit 1
