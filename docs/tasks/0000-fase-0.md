# Tarefa — Fase 0: Fundação

Referência: `PROJECT.md` §17 (Fase 0) e §20 (primeira instrução de execução).

## Escopo autorizado

Bootstrap do repositório DeskPanel: estrutura de pastas, documentação base, esqueletos React/Capacitor e Go, Makefile, lint/format, CI local (`make verify`). Sem funcionalidade real — nenhuma ação de fato executada no Mac, nenhum WebSocket real ainda.

## Checklist

- [x] Repositório git inicializado em `atalho/` (branch `main`).
- [x] `.gitignore` e `.editorconfig`.
- [x] Estrutura de diretórios completa (`apps/android-panel`, `apps/mac-agent`, `configs`, `docs`, `scripts`, `test/fixtures`).
- [x] `README.md` inicial.
- [x] `docs/DECISIONS.md`, `docs/STATUS.md`, `docs/PROTOCOL.md`, `docs/SECURITY.md`, `docs/INSTALL-MACOS.md`, `docs/INSTALL-ANDROID.md`.
- [x] Esqueleto Go (`apps/mac-agent`) compilando.
- [x] Esqueleto React/TS/Vite/Capacitor (`apps/android-panel`) compilando.
- [x] Lint/format configurados nos dois apps.
- [x] `Makefile` com os alvos definidos em `PROJECT.md` §14.1.
- [x] `make verify` rodando limpo.
- [x] `git diff --check` sem erros (fora de arquivos gerados pelo Capacitor).
- [x] Commit inicial: `chore: bootstrap DeskPanel applications`.

## Notas do ambiente

Ver `docs/DECISIONS.md` (ADR-0002) — toolchain Go/pnpm instalada localmente no sandbox usado pela IA, sem root e sem Homebrew (indisponível neste ambiente). No Mac real do usuário, usar Homebrew normalmente.
