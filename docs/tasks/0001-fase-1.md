# Tarefa — Fase 1: Agente e ações locais

Referência: `PROJECT.md` §17 (Fase 1).

## Feito

- `MacOSExecutor` real: open_app, open_url (só http/https), run_shortcut, volume_delta, volume_set, mute_toggle, spotify_control, music_control — via `open`/`shortcuts`/`osascript`, sem shell intermediário, com templates de AppleScript fixos.
- Runner injetável (`runCommandFunc`) para os testes nunca executarem comando real.
- CLI `serve`/`status`/`doctor` carregando e validando `config.json` de verdade (`--config`, default `~/Library/Application Support/DeskPanel/config.json`); `doctor` checa binários e permissão do diretório.
- 36 testes Go passando (`go test ./...`), `make verify` limpo.

## Não feito nesta fase

- `keystroke`, `screen_lock`, `display_sleep` (ficam para depois).
- `serve` de verdade (API/WebSocket) — Fase 2.
